{
  "name": "vst-database",
  "version": "0.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "vst-utils": "workspace:*",
    "@prisma/client": "latest"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "seed": "bun run scripts/seed-db/index.ts"
  },
  "devDependencies": {
    "prisma": "latest"
  }
}
